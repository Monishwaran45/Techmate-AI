import { ProfileSetup } from '../components/auth';

export function ProfileSetupPage() {
  return (
    <div className="min-h-screen bg-gray-50 dark:bg-gray-900 flex items-center justify-center p-4">
      <ProfileSetup />
    </div>
  );
}
